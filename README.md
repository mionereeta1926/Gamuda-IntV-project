# Setup (React + Vite)
## 1) Install dependencies
bash
npm install

## 2) Run the development server
bash
npm run dev

## 3) Open in your browser
After starting, Vite will show a local URL (usually something like http://localhost:5173). Open it to view the app.